{
  "name": "eslint-test-spruce",
  "version": "10.7.4",
  "private": true,
  "description": "Tests for the Spruce Lint packages",
  "license": "MIT",
  "scripts": {
    "test.fail": "cd lint-samples/; for d in fail-*; do echo Trying $d - expecting errors ; eslint $d; if [ $? -ne 1 ]; then echo FAILED: $d; exit 1; fi; echo 👍; done",
    "test.pass": "cd lint-samples/; for d in pass-*; do echo Trying $d - expecting success; eslint $d; if [ $? -ne 0 ]; then echo FAILED: $d; exit 1; fi; echo 👍; done",
    "test": "yarn test.fail && yarn test.pass"
  },
  "devDependencies": {
    "@types/eslint": "^7.2.0",
    "@typescript-eslint/eslint-plugin": "^3.7.1",
    "@typescript-eslint/parser": "^3.7.1",
    "eslint": "^7.5.0",
    "prettier": "^2.0.5",
    "resolve-from": "^5.0.0"
  },
  "dependencies": {
    "eslint-config-spruce": "^10.7.4"
  }
}
